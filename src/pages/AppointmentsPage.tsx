/**
 * AppointmentsPage — paginated list with filters and CRUD actions.
 * Full implementation in Stage 7 (T-64).
 */
export default function AppointmentsPage() {
  return <div>Appointments — Stage 7</div>
}
